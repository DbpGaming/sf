add alias `sf=". sf"` to your shell if you want CD to effect the terminal you call it from, as it has to be ran in the current process.
also set your $MENU variable to what menu you want to use. dmenu and pick both work.
-a hides dot files.